<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Webtoon" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    
    <%# Tailwind CSS CDN for development %>
    <script src="https://cdn.tailwindcss.com"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-white shadow-sm border-b">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <!-- 로고 및 메인 메뉴 -->
          <div class="flex items-center space-x-8">
            <%= link_to "웹툰", root_path, class: "text-xl font-bold text-purple-600" %>
            
            <!-- 메인 네비게이션 -->
            <nav class="hidden md:flex space-x-6">
              <%= link_to "웹툰", "/webtoon", class: "text-gray-900 hover:text-purple-600 font-medium" %>
              <%= link_to "베스트도전", best_challenge_path, class: "text-gray-700 hover:text-blue-600 font-medium" %>
              <%= link_to "도전만화", challenge_path, class: "text-gray-700 hover:text-green-600 font-medium" %>
              <%= link_to "웹소설", novels_path, class: "text-gray-700 hover:text-purple-600 font-medium" %>
            </nav>
          </div>
          
          <!-- 검색 및 사용자 메뉴 -->
          <div class="flex items-center space-x-4">
            <!-- 검색 -->
            <div class="relative hidden md:block">
              <%= form_with url: search_path, method: :get, local: true, class: "relative" do |form| %>
                <%= form.text_field :q, 
                    placeholder: "제목/작가로 검색", 
                    class: "w-64 pl-10 pr-4 py-2 border border-gray-300 rounded-full focus:outline-none focus:ring-2 focus:ring-purple-500",
                    id: "header-search-input" %>
                <button type="submit" class="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-gray-600">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd"></path>
                  </svg>
                </button>
              <% end %>
            </div>
            
            <!-- 사용자 메뉴 -->
            <% if logged_in? %>
              <span class="text-gray-700">안녕하세요, <%= current_user.name %>님!</span>
              <%= link_to "프로필", profile_path, class: "text-gray-600 hover:text-gray-900" %>
              <%= link_to "로그아웃", logout_path, method: :delete, 
                    class: "bg-red-600 hover:bg-red-700 text-white px-3 py-2 rounded-md text-sm font-medium",
                    data: { confirm: "정말 로그아웃하시겠습니까?" } %>
            <% else %>
              <%= link_to "로그인", login_path, class: "text-gray-600 hover:text-gray-900" %>
              <%= link_to "회원가입", signup_path, class: "bg-indigo-600 hover:bg-indigo-700 text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 relative" role="alert">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 relative" role="alert">
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
