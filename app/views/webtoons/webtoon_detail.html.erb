<div class="bg-gray-900 text-white min-h-screen font-sans antialiased">
  <header class="fixed top-0 left-0 right-0 bg-gray-900 bg-opacity-95 z-20 shadow-lg">
    <div class="container mx-auto flex justify-between items-center p-4">
      <a href="/webtoon/list?titleId=<%= @comic.id %>&tab=<%= @comic.day_of_week %>" 
         class="text-xl font-bold truncate hover:text-green-400 transition duration-300 ease-in-out cursor-pointer">
        <%= @comic.title %> - <%= @episode.episode_number %>화
      </a>
      <div class="flex items-center space-x-4">
        <% if @prev_episode %>
          <a href="/webtoon/detail?titleId=<%= @comic.id %>&no=<%= @prev_episode.episode_number %>&week=<%= @comic.day_of_week %>" 
             class="text-gray-300 hover:text-green-400 transition duration-300 ease-in-out text-lg">이전화</a>
        <% else %>
          <span class="text-gray-600 text-lg">이전화</span>
        <% end %>
        
        <% if @next_episode %>
          <a href="/webtoon/detail?titleId=<%= @comic.id %>&no=<%= @next_episode.episode_number %>&week=<%= @comic.day_of_week %>" 
             class="text-gray-300 hover:text-green-400 transition duration-300 ease-in-out text-lg">다음화</a>
        <% else %>
          <span class="text-gray-600 text-lg">다음화</span>
        <% end %>
      </div>
    </div>
  </header>

  <div class="pt-20 pb-20">
    <div class="webtoon-content max-w-4xl mx-auto bg-gray-800 rounded-lg shadow-xl overflow-hidden">
      <!-- 임시 이미지들 -->
      <div class="webtoon-panel">
        <img src="https://picsum.photos/800/1200?random=<%= @episode.id %>1" 
             alt="<%= @comic.title %> <%= @episode.episode_number %>화 - 1" 
             class="w-full h-auto block" loading="lazy">
      </div>
      <div class="webtoon-panel">
        <img src="https://picsum.photos/800/1200?random=<%= @episode.id %>2" 
             alt="<%= @comic.title %> <%= @episode.episode_number %>화 - 2" 
             class="w-full h-auto block" loading="lazy">
      </div>
      <div class="webtoon-panel">
        <img src="https://picsum.photos/800/1200?random=<%= @episode.id %>3" 
             alt="<%= @comic.title %> <%= @episode.episode_number %>화 - 3" 
             class="w-full h-auto block" loading="lazy">
      </div>
      <div class="webtoon-panel">
        <img src="https://picsum.photos/800/1200?random=<%= @episode.id %>4" 
             alt="<%= @comic.title %> <%= @episode.episode_number %>화 - 4" 
             class="w-full h-auto block" loading="lazy">
      </div>
      <div class="webtoon-panel">
        <img src="https://picsum.photos/800/1200?random=<%= @episode.id %>5" 
             alt="<%= @comic.title %> <%= @episode.episode_number %>화 - 5" 
             class="w-full h-auto block" loading="lazy">
      </div>
    </div>
  </div>

  <!-- 댓글 섹션 -->
  <div class="max-w-4xl mx-auto px-4 py-8">
    <div class="bg-gray-800 rounded-lg p-6">
      <h3 class="text-xl font-bold text-white mb-6 flex items-center">
        <span class="text-blue-400 mr-2">💬</span>
        댓글 <span class="text-gray-400 text-sm ml-2">(12)</span>
      </h3>
      
      <!-- 댓글 작성 -->
      <div class="mb-6">
        <div class="flex space-x-3">
          <div class="w-10 h-10 bg-purple-600 rounded-full flex items-center justify-center text-white font-bold">
            U
          </div>
          <div class="flex-1">
            <textarea placeholder="댓글을 입력하세요..." 
                      class="w-full bg-gray-700 text-white rounded-lg p-3 resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
                      rows="3"></textarea>
            <div class="flex justify-between items-center mt-2">
              <span class="text-gray-400 text-sm">최대 500자</span>
              <button class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition duration-200">
                댓글 작성
              </button>
            </div>
          </div>
        </div>
      </div>
      
      <!-- 댓글 목록 -->
      <div class="space-y-4">
        <!-- 댓글 1 -->
        <div class="flex space-x-3">
          <div class="w-8 h-8 bg-green-600 rounded-full flex items-center justify-center text-white text-sm font-bold">
            김
          </div>
          <div class="flex-1">
            <div class="flex items-center space-x-2 mb-1">
              <span class="text-white font-medium text-sm">김웹툰</span>
              <span class="text-gray-400 text-xs">2시간 전</span>
            </div>
            <p class="text-gray-300 text-sm">이번 화 진짜 대박이네요! 다음 화가 너무 궁금해요 ㅠㅠ</p>
            <div class="flex items-center space-x-4 mt-2">
              <button class="text-gray-400 hover:text-blue-400 text-xs flex items-center">
                <span class="mr-1">👍</span> 좋아요 (5)
              </button>
              <button class="text-gray-400 hover:text-blue-400 text-xs">답글</button>
            </div>
          </div>
        </div>
        
        <!-- 댓글 2 -->
        <div class="flex space-x-3">
          <div class="w-8 h-8 bg-red-600 rounded-full flex items-center justify-center text-white text-sm font-bold">
            이
          </div>
          <div class="flex-1">
            <div class="flex items-center space-x-2 mb-1">
              <span class="text-white font-medium text-sm">이독자</span>
              <span class="text-gray-400 text-xs">3시간 전</span>
            </div>
            <p class="text-gray-300 text-sm">작가님 그림체가 점점 더 좋아지는 것 같아요! 스토리도 흥미진진하고</p>
            <div class="flex items-center space-x-4 mt-2">
              <button class="text-gray-400 hover:text-blue-400 text-xs flex items-center">
                <span class="mr-1">👍</span> 좋아요 (3)
              </button>
              <button class="text-gray-400 hover:text-blue-400 text-xs">답글</button>
            </div>
          </div>
        </div>
        
        <!-- 댓글 3 -->
        <div class="flex space-x-3">
          <div class="w-8 h-8 bg-yellow-600 rounded-full flex items-center justify-center text-white text-sm font-bold">
            박
          </div>
          <div class="flex-1">
            <div class="flex items-center space-x-2 mb-1">
              <span class="text-white font-medium text-sm">박팬</span>
              <span class="text-gray-400 text-xs">5시간 전</span>
            </div>
            <p class="text-gray-300 text-sm">주인공 캐릭터가 정말 매력적이에요. 응원합니다!</p>
            <div class="flex items-center space-x-4 mt-2">
              <button class="text-gray-400 hover:text-blue-400 text-xs flex items-center">
                <span class="mr-1">👍</span> 좋아요 (8)
              </button>
              <button class="text-gray-400 hover:text-blue-400 text-xs">답글</button>
            </div>
          </div>
        </div>
      </div>
      
      <!-- 더보기 버튼 -->
      <div class="text-center mt-6">
        <button class="text-blue-400 hover:text-blue-300 text-sm font-medium">
          댓글 더보기 ▼
        </button>
      </div>
    </div>
  </div>

  <footer class="fixed bottom-0 left-0 right-0 bg-gray-900 bg-opacity-95 z-20 shadow-lg py-4">
    <div class="container mx-auto flex justify-center">
      <% if @next_episode %>
        <a href="/webtoon/detail?titleId=<%= @comic.id %>&no=<%= @next_episode.episode_number %>&week=<%= @comic.day_of_week %>" 
           class="bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-8 rounded-full text-lg transition duration-300 ease-in-out transform hover:scale-105">
          다음화 보기
        </a>
      <% else %>
        <button onclick="scrollToComments()" 
                class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-8 rounded-full text-lg transition duration-300 ease-in-out transform hover:scale-105">
          댓글 보기
        </button>
      <% end %>
    </div>
  </footer>
</div>